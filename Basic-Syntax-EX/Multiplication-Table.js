function solve(integer) {

    for (let i = 1; i <= 10; i++) {
       
       let result = integer*i;
       console.log(`${integer} X ${i} = ${result}`);
        
    }
}

solve(5);
 solve(11);